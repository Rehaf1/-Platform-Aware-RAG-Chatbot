# Base URL of your FastAPI backend's API routes.
# If you're running the backend via docker compose, it's still exposed on your
# host at 127.0.0.1:8000 (from the port mapping), so this stays the same.
VITE_API_BASE=http://127.0.0.1:8000/api/v1
